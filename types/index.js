/* eslint-disable no-unused-vars */

const { Language } = require('./language')

/**
 * Example Type Definition.
 *
 * @typedef {Object} ExampleType
 * @property {number} exampleNumber - An Example Number Property
 * @property {string} exampleString - An Example String Property
 * @property {Language} language - An example of how to import from other type files.
 */
exports.ExampleType = this.ExampleType
